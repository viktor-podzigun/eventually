name: CI

on:
  push:
    paths-ignore:
      - "*.md"
    branches: [main]
    tags:
      - "*"
  pull_request:
    branches: [main]

permissions:
  id-token: write # Required for OIDC
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Git checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "24"
          registry-url: "https://registry.npmjs.org"

      - name: Cache node_modules
        uses: actions/cache@v4
        with:
          path: |
            ~/.npm
            ~/.nvm
            ~/work/eventually/eventually/node_modules
            ~/work/eventually/eventually/package-lock.json
          key: ${{ runner.os }}-node_modules-cache-v1-${{ hashFiles('package.json') }}
          restore-keys: |
            ${{ runner.os }}-node_modules-cache-v1-

      - name: Extract Tag Name
        run: echo "TAG_NAME=$(echo ${GITHUB_REF##*/})" >> $GITHUB_ENV
        if: ${{ startsWith(github.ref, 'refs/tags') }}

      - name: Run tests
        run: |
          npm i
          npm run formatCheck
          npx quick-lint-js ./src/*.mjs ./test/*.mjs
          npx tsc
          NODE_V8_COVERAGE=coverage0 npx c8 -r lcovonly --all --src ./src node --experimental-test-coverage ./test/all.mjs
        if: ${{ env.TAG_NAME == '' }}

      - name: Report coverage to Coveralls
        uses: coverallsapp/github-action@v2
        if: ${{ env.TAG_NAME == '' }}

      - name: Build SNAPSHOT
        run: npm pack
        if: ${{ env.TAG_NAME == '' }}

      - name: Publish RELEASE
        run: |
          VERSION="$(echo "$TAG_NAME" | cut -d'v' -f 2)"
          echo "Publish a release version=$VERSION for tag $TAG_NAME"
          npm --no-git-tag-version --allow-same-version version $VERSION
          npm pack
          npm publish eventually-${VERSION}.tgz
        if: ${{ env.TAG_NAME != '' }}
